# Website for lyon-fnal.github.io

To update source, must be on the `sources` branch. 
Update the site and push `public` directory (along with everything else). 

Then `git subtree push --prefix=public origin master`
